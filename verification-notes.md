# CondoCore Verification Notes

The live preview opened successfully at the active development URL. The dashboard renders the fixed CondoCore command rail, the route-aware generated blueprint banner, the dashboard header, the full portfolio metric cards, the project pulse table with all four mock projects, this-week activity, sales velocity, and upcoming milestone content.

The Project Plan route opened through the sidebar at `#/plan`. It renders the generated blueprint banner, task summary metrics, the Timeline/Kanban/List controls, status and owner filters, time-scale controls, and the timeline table seeded with the uploaded mock task data, including the 42-task count and planning task rows.

The Stacking Plan route opened through the sidebar at `#/stacking`. It renders the generated blueprint banner, sellout metrics, status filters, a complete floor-by-floor stack with roof/mechanical, penthouse, floors 1–4, cellar/common areas, and selectable unit buttons using the uploaded mock unit data.

The Document Vault route opened through the sidebar at `#/vault`. It renders the generated vault banner, folder counts, category counts, storage meter, search and type/date filters, and the visible document table rows seeded from the uploaded mock document data, including offering plan, DOB approval, GC contract, drawing, shop drawing, and steel-pricing records.

The Financials route opened through the sidebar at `#/financials`. It renders the generated finance-ledger banner, FY selector, hard-cost/commitment/spend/expense/variance metric cards, Budget/Expenses/Capital Stack/Draws tabs, and the CSI budget table with the uploaded mock line items and variance markers.

The Vendors route opened through the sidebar at `#/vendors`. It renders the generated finance-ledger banner, total vendors/committed value/paid-to-date/COI alert metrics, search, trade filters, table/cards controls, and visible vendor records including Wonder Works, ZProekt, GACE, ME Engineers, and Buro Happold from the uploaded mock data.

The Project Details route opened through the sidebar at `#/details`. It renders the generated blueprint banner, project summary metrics, Overview/Site/Compliance/Amenities/Units tabs, site and acquisition facts, plan completion, building amenities, and regulatory/compliance records from the uploaded mock data.
